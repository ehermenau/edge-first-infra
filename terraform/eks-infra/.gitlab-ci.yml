include:
  - local: .gitlab-ci/terraform.yml

variables:
  TF_ROOT: "eks-infra"

tf_plan_eks:
  extends: .tf_plan

staging_apply_eks:
  extends: .staging_apply

prod_apply_eks:
  extends: .prod_apply
